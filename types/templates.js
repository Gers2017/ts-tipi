const templates = {
  tsTemplate: "ts-template",
  expressTemplate: "express",
  apolloServerTemplate: "apollo-server",
};

const templateChoices = [
  templates.tsTemplate,
  templates.expressTemplate,
  templates.apolloServerTemplate,
];

module.exports = {
  templates,
  templateChoices,
};
